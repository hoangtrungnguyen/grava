# plan/

Phase plans, epics, and story breakdowns for upcoming work. `phase2B/` holds the active phase. `_bmad/` and `_bmad-output/` are BMad-generated artifacts (gitignored). Use this directory for forward-looking planning; completed work belongs in `tracker/`.
