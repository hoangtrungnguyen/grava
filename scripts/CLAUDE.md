# scripts/

Shell scripts and SQL helpers for build, release, Dolt setup, hooks, schema management, and dev/test environment bootstrap. Entry points include `build.sh`, `release.sh`, `start_dolt_server.sh`, `setup_test_env.sh`, `install.sh`. Subdirs `hooks/`, `schema/`, `test/` hold supporting assets.
