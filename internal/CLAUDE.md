# internal/

Private packages, non-importable outside this module (Go `internal/` rule). Currently hosts `testutil` for shared test helpers. Place code here only when it must be hidden from external consumers.
